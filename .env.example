# SafeLog AI — app environment (Rails analyze, encryption, deploy).
# Copy to .env and fill in. Never commit .env.

# Optional — omit or leave blank to use Ai::FakeClient (sample analyze output in dev/production)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Rails Active Record Encryption (development/test — see config/credentials or deploy-plan)
# ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=
# ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
# ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=
