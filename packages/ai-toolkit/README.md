# @szymoniwacz/ai-toolkit

Team AI artifacts (Cursor skills and agent rules) distributed through GitHub Packages.

## Status

Package skeleton (Phase 1). Install scripts, skill content, and publish workflow follow in later phases.

## Consumer setup (preview)

```text
@szymoniwacz:registry=https://npm.pkg.github.com
```

Do not commit auth tokens to `.npmrc`. Use `npm login` locally or `GH_PKG_TOKEN` in CI.
