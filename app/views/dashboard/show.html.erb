<h1>SafeLog AI</h1>
<p>Signed in as <%= current_user.email %>.</p>
<p>
  <%= link_to "Debugging cases", debugging_cases_path %>
  · <%= link_to "New debugging case", new_debugging_case_path %>
  <% if Demo::LoadCase.available? %>
    · <%= button_to "Load demo case", load_demo_debugging_cases_path, method: :post %>
  <% end %>
</p>
<p>Create a case with multiple pasted log sources. SafeLog redacts sensitive values in memory and stores sanitized evidence only.</p>
